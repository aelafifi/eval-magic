{
  "name": "eval-magic",
  "version": "0.0.1-alpha.3",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build:node": "esbuild ./src/index.ts --bundle --outfile=index.js --packages=external --platform=node",
    "build:browser": "esbuild ./src/index.ts --bundle --outfile=index.js --platform=browser --global-name=executejs --minify",
    "build:all": "npm run build && npm run build:browser",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:coverage:watch": "jest --coverage --watch"
  },
  "keywords": [],
  "author": "ahmed.s.elafifi@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/escodegen": "^0.0.10",
    "@types/jest": "^29.5.12",
    "esbuild": "0.25.0",
    "jest": "^29.7.0",
    "prettier": "^3.3.3",
    "ts-jest": "^29.1.2",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "acorn": "^8.12.1",
    "escodegen": "^2.1.0",
    "estree-walk-plus": "^0.0.2"
  }
}
